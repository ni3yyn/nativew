name: Build Standalone Debug APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths-ignore:
      - 'README.md'

jobs:
  build:
    name: 🤖 Build Android Standalone APK
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'

      - name: 🐘 Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: false

      - name: 📦 Install Dependencies
        run: npm install

      - name: ⚙️ Expo Prebuild
        run: npx expo prebuild --platform android

      - name: 🔨 Build APK (Offline & Small)
        run: |
          cd android
          chmod +x gradlew
          # -PbundleInDebug=true -> Bundles JS (Offline support)
          # -Pandroid.injected.build.abi=arm64-v8a -> Small size
          ./gradlew assembleDebug -PbundleInDebug=true -Pandroid.injected.build.abi=arm64-v8a --no-daemon --build-cache

      - name: 📤 Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-standalone
          # CHANGED: Use *.apk to catch whatever name Gradle gives it (likely app-arm64-v8a-debug.apk)
          path: android/app/build/outputs/apk/debug/*.apk